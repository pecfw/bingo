///////////////////////
/////   Restart   //////
///////////////////////

var RestartGame = (props) => {
    return (
      <button >
        {/* onClick={store.dispatch({ type: "RESTART_GAME", value: '' })}> */}
        Restart Game
      </button>
    )
  }